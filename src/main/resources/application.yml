# server
server:
  port: '8088'

# spring
spring:
  application:
    name: 'Support-svc'

# mybatis
mybatis:
  configuration:
    mp-underscore-to-camel-case: true

# meowu
meowu:
  web:
    cors: 'true'
    converter: 'gson'

# logging
logging:
  config: 'classpath:logback-spring.xml'