# spring
spring:
  application:
    name: 'Support-svc'

# mybatis
mybatis:
  configuration:
    mp-underscore-to-camel-case: 'true'
    log-impl: 'org.apache.ibatis.logging.stdout.StdOutImpl'

# logging
logging:
  config: 'classpath:logback-spring.xml'